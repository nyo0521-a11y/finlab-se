{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{ range .Pages }}
  {{- /* head.html で noindex にしているページ（タグ一覧・タグ個別・robotsNoIndex 指定）はサイトマップからも除外し、シグナルの矛盾を防ぐ */ -}}
  {{- $isTagPage := and (in (slice "term" "taxonomy") .Kind) (eq (default "" .Data.Plural) "tags") -}}
  {{- $isNoIndex := eq .Params.robotsNoIndex true -}}
  {{- if not (or $isTagPage $isNoIndex) }}
  <url>
    <loc>{{ .Permalink }}</loc>{{ if not .Lastmod.IsZero }}
    <lastmod>{{ safeHTML ( .Lastmod.Format "2006-01-02T15:04:05-07:00" ) }}</lastmod>{{ end }}{{ with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>{{ end }}{{ if ge .Sitemap.Priority 0.0 }}
    <priority>{{ .Sitemap.Priority }}</priority>{{ end }}{{ if .IsTranslated }}{{ range .Translations }}
    <xhtml:link
      rel="alternate"
      hreflang="{{ .Language.LanguageCode }}"
      href="{{ .Permalink }}"
      />{{ end }}
    <xhtml:link
      rel="alternate"
      hreflang="{{ .Language.LanguageCode }}"
      href="{{ .Permalink }}"
      />{{ end }}
  </url>
  {{- end }}
  {{ end }}
</urlset>
